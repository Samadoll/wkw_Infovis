let regionMap = new RegionMap({ parentElement: "#project-map", containerWidth: 600, containerHeight: 450 });
let typeGraph = new RegionMap({ parentElement: "#project-type", containerWidth: 600, containerHeight: 450 });
regionMap.update();
typeGraph.update();
